import { NextRequest, NextResponse } from "next/server";import { db } from "@/lib/server-db";import { getSessionUser } from "@/lib/server-auth";
export async function GET(){const u:any=await getSessionUser();if(!u)return NextResponse.json({message:"Unauthorized"},{status:401});const r=await db.execute({sql:"SELECT id,data_json FROM journal_posts WHERE account_id=? OR family_id=?",args:[u.account_id,u.family_id]});return NextResponse.json({items:r.rows.map((x:any)=>JSON.parse(String(x.data_json)))});}
export async function POST(req:NextRequest){const u:any=await getSessionUser();if(!u)return NextResponse.json({message:"Unauthorized"},{status:401});const b=await req.json();const id=b.id||crypto.randomUUID();const now=new Date().toISOString();await db.execute({sql:"INSERT INTO journal_posts (id,account_id,family_id,visibility,data_json,created_at,updated_at) VALUES (?,?,?,?,?,?,?)",args:[id,u.account_id,b.visibility==="private"?null:u.family_id,b.visibility||"private",JSON.stringify({...b,id}),now,now]});return NextResponse.json({item:{...b,id}});}
